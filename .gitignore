# dependencies
node_modules

# build output
dist
*.tsbuildinfo

# logs
*.log
npm-debug.log*

# env files — never commit secrets (API keys, registry credentials, etc.)
.env
.env.*
!.env.example

# editor / OS cruft
.DS_Store
.vscode
.idea
*.swp

# local-only tool settings (machine-specific permissions, not shared config)
.claude/settings.local.json
